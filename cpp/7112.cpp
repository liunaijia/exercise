#include <iostream>
#include <string>

struct boss
{
};

struct map
{
	/* data */
};

int main()
{
	void igor();
	float fofu(int);
	double mpg(double, double);
	long summation(long[], int);
	double doctor(const std::string);
	void ofcourse(boss);
	std::string plot(map);

	return 0;
}